# `functions/`

Aquesta carpeta conté les **Firebase Cloud Functions** del projecte, pensades per donar suport a la lògica del núvol i enviar notificacions als usuaris.

Consulta el `README.md` principal del repositori per veure una descripció general del projecte i els punts d'accés.

