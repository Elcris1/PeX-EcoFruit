package com.example.ecofruit.ui.theme

import androidx.compose.ui.graphics.Color

// ── Verdes principales ──
val EcoForest  = Color(0xFF1A3D2B)   // Fondo oscuro, headers, navbar
val EcoMoss    = Color(0xFF2E6B45)   // Primario, botones CTA, precios
val EcoLeaf    = Color(0xFF4A9B6A)   // Interactivo, hover, iconos activos
val EcoSpring  = Color(0xFF72C48A)   // Hover claro, badges, highlights
val EcoMint    = Color(0xFFB8E8CA)   // Fondos suaves, bordes, separadores
val EcoFoam    = Color(0xFFE8F7EE)   // Fondo de página, superficies

// ── Tonos tierra ──
val EarthSoil  = Color(0xFF3D2B1A)   // Texto oscuro secundario
val EarthClay  = Color(0xFF8B5E3C)   // Acento cálido, categorías
val EarthSand  = Color(0xFFD4A96A)   // Acento dorado, premium
val EarthCream = Color(0xFFFAF3E0)   // Fondo cálido alternativo

// ── Acentos de estado ──
val FruitTomato = Color(0xFFE85D3A)  // Alertas, ofertas urgentes, stock bajo
val FruitLemon  = Color(0xFFF5C518)  // Destacados, badges "nuevo"
val FruitPlum   = Color(0xFF6B3FA0)  // Categoría premium / ecológico cert.

// ── Neutros ──
val NeutralDark  = Color(0xFF111A14)
val NeutralMid   = Color(0xFF4A5C4E)
val NeutralLight = Color(0xFFC8D5CB)
val NeutralWhite = Color(0xFFFAFFFE)